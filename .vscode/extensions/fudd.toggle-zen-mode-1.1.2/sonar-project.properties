# SonarCloud: 'Sonarcloud fudd@fuddstar'
#     organization: 'azure-devops'
#     scannerMode: 'CLI'
#     configMode: 'manual'
#     cliProjectKey: 'azure-devops'
#     cliProjectName: 'toggle-zen-mode'
#     cliSources: '.'


# sonar-project.properties
# must be unique in a given SonarQube instance
sonar.projectKey=azure-devops

# --- optional properties ---

# defaults to project key
sonar.projectName=toggle-zen-mode
# defaults to 'not provided'
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=src

# Path to Tests and coverage results
sonar.tests=test 

# Encoding of the source code. Default is default system encoding
# sonar.sourceEncoding=UTF-8

# Typescript tsconfigPath JSON file
# sonar.typescript.tsconfigPath=.
